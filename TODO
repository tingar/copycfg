Pruning:
  - Needs to support a prune option
  - Specify a list of netgroups, and remove everything not in a netgroup
  - Have a --backups option to prune backups as well. Maybe.
Configuration:
  - Needs default configuration location
